# 小说/漫剧链式 Agent：官网维护规则

## 范围与授权

- 本文件仅约束本公开网站仓库。保留宿主全局安全、中文沟通、模型路由及编码要求；本仓库不是创作入口，维护网站不触发小说改编、导演、生图或其他 Skill。
- 解释、诊断只做相关只读检查；明确要求修改时，自主完成范围内实现及适度本地验证。只在关键输入缺失或越过授权边界时询问，不逐文件重复确认。
- 保留已有未提交改动。不修改全局、仓库外或 Skills，除非另获明确授权；建议放在仓库内文档，不把建议当已生效规则。
- 本地修改不等于获准提交、推送、部署、发布、安装、发送消息或删除重要数据。尤其推送 `main` 会触发 Pages 部署，需发布授权。
- 不读取凭据、会话记录、缓存或扫描无关文件；不自动写仓库外记忆。公开内容不得包括私有 Skill 正文、凭据、源路径清单或未授权素材。

## 数据与发布约束

- 中文名为“小说/漫剧链式agent”。网站是公开指南，安装包在私有仓库；访问者仍需要 GitHub 权限及浏览器登录，不能因维护者 CLI 可下载就宣称所有访客可下载。
- `data/` 发行数据由套件的 `release/update-history.json` 和下载目录生成，禁止独立编造版本、时间、组件变化、更新说明或哈希。
- 网站展示最近三个稳定版本（含当前）；GitHub 至少保留当前加三个旧版，默认不删历史。真实版本不足时说明，不补假链接。
- 不启用每小时检测。优化完成且验证通过后询问同步；用户确认前不发布。网站与 Release 必须同源，跨仓库发布失败时保留旧网站并报告待完成步骤，不宣称原子同步成功。
- 更新不能绕过用户安装确认。安装后的重载、回退和离线使用说明不得承诺当前宿主不支持的行为。

## 验证与交付

- HTML/CSS 文案改动检查相关结构与链接；JS 改动执行语法及相关测试；发行数据改动检查版本顺序、数量、链接、哈希和统一说明一致性。无需每次运行创作链、所有 Skill 测试或多 Agent。
- 使用 `pwsh` 和显式 UTF-8，保留中文文件源名。常用命令：`git status --short`、`git diff --check`、`node --check assets/site.js`。
- 数据读取失败应显示明确提示且不使其他区块一起失效；不把更新说明字符串或错误消息当可信 HTML 执行。
- 完成后简要给出改动、实际验证和未验证项。本地静态检查不能代替真实浏览器私有下载、Pages 部署及客户端重载验证。
